import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { IAnalyzerRecognizers } from '@/shared/model/analyzer-recognizers.model';

const baseApiUrl = 'api/analyzer-recognizers';

export default class AnalyzerRecognizersService {
  public find(id: number): Promise<IAnalyzerRecognizers> {
    return new Promise<IAnalyzerRecognizers>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IAnalyzerRecognizers): Promise<IAnalyzerRecognizers> {
    return new Promise<IAnalyzerRecognizers>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IAnalyzerRecognizers): Promise<IAnalyzerRecognizers> {
    return new Promise<IAnalyzerRecognizers>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: IAnalyzerRecognizers): Promise<IAnalyzerRecognizers> {
    return new Promise<IAnalyzerRecognizers>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
